This is a attendace tracker project
Use simple code jnr dev can understand
Comment so as it is easy to read the code
Comment on places one can esaily change to other values so that one can easily do that 
The appliaction should be acessed form main.py


TASK
When edit student data is cliked in root.py
Let it open chooseviewer.py A window that allows the user to choose a student data file to view or edit form db/allstudents (It should be dynami such that it is the files that is there that should show on th gui for the user to select)
Then when the user selects a file it should open viewergui.py(It should be liike a stnadrd excel application where user can edit, cells, save and do other major fucntionlity that one would expect make it comprehesive and use as much libries as possible)
Update requirements.txt 
